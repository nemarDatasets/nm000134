* annex.backend=MD5E
**/.git* annex.largefiles=nothing
.gitignore annex.largefiles=nothing
# Stimuli (JPGs) go through git-annex so the binaries land on nemar-s3
# rather than bloating the GitHub repo.
